To create mini indices run parser.py
These mini indices are created on batches of docs
Each batch has 100k docs
To merge them run merger.py
After merging new merged indexes will be created
Title_parser.py gets the titles from the dump and stores them
This is used while searching to get title names
